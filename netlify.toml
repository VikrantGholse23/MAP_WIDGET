[build]
  command = "npm run build"
  publish = "dist/survey-map-app/browser"

# SPA fallback: serve index.html for all routes so Angular router works
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
